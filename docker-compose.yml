services:
  web:
    image: nginx:alpine
    ports: ["8080:80"]
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:80 >/dev/null 2>&1"]
      interval: 10s
      timeout: 3s
      retries: 3
